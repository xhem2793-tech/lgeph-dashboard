# 본사 제출 자가점검 — axlgeph.report

_작성: 2026-08-05 (야간 자동점검) · 대상: AX 서밋 제출용 검토_

---

## 1. 종합 평가 — 제출 적합한가?

**결론: 제출 가능. 상위권 사례가 될 수 있음.** 단, 아래 "제출 전 점검"의 소수 항목을 정리하면 완성도가 크게 올라감.

**강점 (왜 상위권인가)**
- **완성형 운영 제품** — 데모가 아니라 자체 도메인(axlgeph.report)에서 상시 운영. 인증·개인화·일 단위 자동 수집까지 엔드투엔드.
- **실데이터 규모** — 경쟁사 가격 누적 7.5만 행(일 4.6천)·거시지표 210+·DOE 에너지라벨·경쟁광고. 스크린샷용 목업이 아님.
- **의사결정 밀착** — 채널별 가격 매트릭스·일일 변동·포지셔닝·이상치 알림 등 "그래서 뭘 하나(so-what)"에 답하는 뷰.
- **글로벌 대응** — 한/영 토글(UI·데이터 모두), 다크모드, 반응형.
- **기술 성숙도** — Next.js 정적 export + Supabase + Cloudflare(서버리스, git push=배포), 세션 캐시로 재방문 즉시화.

**한 줄 포지셔닝**: "n8n이 배관이라면, 이건 그 위에서 매일 돌아가는 완성 제품."

---

## 2. 오늘 밤 즉시 수정 완료

- ✅ **메타데이터 실브랜딩화** — 플레이스홀더("Tremor OSS Dashboard·yoururl.com·@tremorlabs·yourname") → LGE-PH로 교체 + `robots: noindex`(사내 비공개).
- ✅ **성능** — Supabase 세션 캐시(재방문 재조회 0)·정적자산 영구캐시·보안헤더·이미지 lazy.
- ✅ **영문화** — UI 전면 + 데이터(캘린더·지표·배너) 영문 연결.

---

## 3. 제출 전 점검·수정 권장

| 심각도 | 항목 | 상태/조치 |
|---|---|---|
| 中 | **데드코드 정리** — 숨긴 `/overview` + recharts·datepicker·react-day-picker 의존성 | 라우트 분리라 런타임 영향은 적으나, 제출 전 삭제하면 번들·인상 깔끔. (요청 시 정리) |
| 中 | **아젠다 일부 이벤트 EN 누락** — DB `event_en=null`인 소수 일정 | 데이터 보정(해당 행 EN 채우기)로 해결. 코드 아님. |
| 中 | **"예정(SOON/PLAN)" 기능 다수** — 갭분석·변동성·경쟁강도·환율연동·so-what 요약 등 | 데모 시 "로드맵상 예정"으로 프레이밍하면 오히려 강점. 라이브 1~2종 추가하면 더 좋음. |
| 低 | **이벤트 상세(summary/implication)·마케팅 광고 원문** = 한글 데이터(EN 컬럼 없음) | 뉴스처럼 DB EN 컬럼 추가 시 영문화 가능(대량 작업). 현재는 원문 유지 정책. |
| 低 | **competitors 첫 로드 요청 31건**(병렬) | 기능상 불가피(13.8천 행). 캐시로 재방문 즉시. 필요 시 서버 집계 뷰로 경량화 여지. |
| 低 | **favicon/OG 이미지** — 기본 아이콘 | LG/브랜드 파비콘·OG 카드 제작 시 완성도↑. |

> **데모 시나리오 권장**: 국가동향(뉴스·인사이트) → 시장동향(채널별 가격·이상치) → 주요지표(거시 선행) → 로드맵(예정 기능) 순. "실데이터·매일 갱신"을 초반에 각인.

---

## 4. 디자인 철학 (문서화)

우리 대시보드의 시각·인터랙션 원칙 — 일관되게 적용됨.

1. **색 = 신호(Signal, not decoration)** — 색은 장식이 아니라 의미. 유리(기회)=emerald, 불리·경보=rose, 주의=amber, 자사(LG)=indigo. 틴트 카드 남발 금지, AI 분석은 좌측 인디고 바 + 신호 dot.
2. **의도된 모션** — 진입 `fadeUp`(0.2~0.5s), 스프링 이징 `cubic-bezier(.34,1.42,.64,1)`, hover는 색만(indigo) 전환, click은 `active:scale`로 살짝 눌림. 알약 토글·모달·사이드바 통일. "튀지 않되 살아있게."
3. **정보 밀도 + 여백** — `tabular-nums`로 숫자 정렬, 조밀하지만 스캔 가능한 표. 과밀 시 좌측 색 스트립·미니 스파크라인으로 "숫자를 모양으로" 읽게.
4. **한/영·라이트/다크 네이티브** — 처음부터 이중언어·양 테마 전제(사후 부착 아님).
5. **운영 제품의 톤** — "베타" 배지·최신 갱신 스탬프·CONFIRMED 표식으로 "살아있는 데이터"를 지속 신호.
6. **로직과 표시의 분리** — 필터 키(한글)는 로직, 표시만 번역/약자화(REF·W/M). 데이터 무결성 우선.

---

## 5. 추가 제안 — 지표 / 페이지 / 데이터

**A. 지표(무료·keyless 소스 확보됨 — 메모리 참조)**
- **PhilGEPS 조달** — B2B(사이니지·모니터·공조) 공공조달 파이프라인.
- **Comtrade HS 코드** — 852852/8531(사이니지)·8528(모니터) 수입 흐름 → B2B 수요 선행.
- **DPWH 인프라 예산** — 지역별 건설·인프라 → 빌트인·B2B 상관.
- **PSA OpenSTAT / Open-Meteo / USGS** — 지역 물가·기상·재난(이미 일부).

**B. 신규 페이지·뷰**
- **B2B 디스플레이·공조 시장** — 사이니지/모니터/HVAC 별도 도메인(현재 CE 중심).
- **가격 변동성·경쟁강도 지수** — 이미 로드맵 'plan' → 라이브화(데이터는 있음).
- **환율 연동 원가 분석** — 페소 약세 ↔ 수입가전 COGS 상관(지표 존재, 뷰만).
- **경쟁분석 so-what 요약** — 핵심 인사이트 + 액션(Owner·Timing) 자동 롤업.
- **프로모 캘린더** — 유통사 세일(8.8·9.9 등) + 급여일·전기요금 트리거 통합 타임라인.

**C. 데이터 심화(있으면 격 상승, 확보 난이도 있음)**
- **판매량·점유율** — GfK 패널(유료·비공개)이 있어야 진짜 시장점유. 없으면 "가격·노출 프록시"임을 명시.
- **소셜 리스닝** — 브랜드 언급·감성(광고 효과 검증).
- **오프라인 트래픽/전시** — 매장 진열·전시 커버리지(부분 있음).

---

## 6. 리스크·한계 (정직하게)

- **수집기(Python) 유지보수** — 개인 스크립트 아닌 GitHub Actions 운영이나, 조직 이관 위해 문서화 필요(로드맵 Phase 2).
- **데이터 신선도 편차** — 소스별 갱신 주기 상이(일/월/분기). 각 뷰에 기준일 명시로 대응 중.
- **판매/점유 부재** — 위 5-C. 데모 시 선제 설명 권장.
- **무료 SMTP 한도** — Supabase 로그인 이메일 발송량 많으면 사내 SMTP 필요.
