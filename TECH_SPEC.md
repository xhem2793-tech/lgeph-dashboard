# axlgeph.report 기술서

**LGE-PH 시장 인텔리전스 플랫폼 — 시스템 기술 명세서**

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.0 |
| 작성일 | 2026-08-04 |
| 서비스 | axlgeph.report |
| 소관 | LGE-PH 경영기획 |
| 분류 | 사내 한정 (Cloudflare Access 인증) |

---

## 1. 개요 (Executive Summary)

본 시스템은 **필리핀 거시경제·경쟁사·유통·마케팅 데이터를 매일 자동 수집·정제하여
경영기획 의사결정에 즉시 활용 가능한 형태로 시각화**하는 웹 기반 인텔리전스 플랫폼이다.

- **성격** — 일회성 데모나 스크립트가 아니라, 자체 도메인에서 **상시 운영 중인 제품(product)**
- **범위** — 국가동향·시장동향·주요지표·마케팅·리포트 5개 도메인
- **차별점** — 데이터 수집(자동화)부터 저장·시각화·인증·개인화까지 **엔드투엔드 완결**

> 대부분의 현업 AX 사례가 "특정 작업 1건의 자동화(점 자동화)"에 머무는 반면,
> 본 시스템은 **데이터 파이프라인 + 운영 서비스**를 모두 갖춘 **플랫폼**이다.

---

## 2. 시스템 아키텍처

### 2.1 레이어 구성

```
┌─────────────────────────────────────────────────────────────────────┐
│  ① 프레젠테이션 (제품)                                                 │
│     Next.js (React / TypeScript) · Tailwind CSS · 정적 빌드            │
│     한/영 이중언어 · 다크모드 · 계정별 개인화                          │
├─────────────────────────────────────────────────────────────────────┤
│  ② 배포 / 인증                                                         │
│     Cloudflare Pages (git push → 자동 배포)                            │
│     Cloudflare Access (이메일 OTP · 화이트리스트 · 세션 관리)          │
├─────────────────────────────────────────────────────────────────────┤
│  ③ 데이터 저장                                                         │
│     Supabase (PostgreSQL) · REST API · 뷰(View) 기반 서빙             │
├─────────────────────────────────────────────────────────────────────┤
│  ④ 데이터 수집                                                         │
│     Python 스크래퍼 · GitHub Actions 스케줄 실행                       │
│     (버전관리 · 실행로그 · 자동 재시도)                                │
├─────────────────────────────────────────────────────────────────────┤
│  데이터 소스                                                           │
│     World Bank · BSP · PSA · 경쟁사 7개 거래선 · DOE · 경쟁광고        │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 데이터 흐름

```
소스 수집 → 정제·분류(카테고리/모델 매칭) → Supabase 적재 → 뷰 집계 → 대시보드 렌더
   (④)              (④)                        (③)          (③)         (①)
```

---

## 3. 기술 스택 상세

| 레이어 | 기술 | 선택 이유 |
|---|---|---|
| 프론트엔드 | **Next.js (App Router) + TypeScript** | 업계 표준 · 정적 export로 서버 불필요 · 타입 안정성 |
| 스타일 | **Tailwind CSS** | 일관된 디자인 시스템 · 유지보수 용이 |
| 데이터베이스 | **Supabase (PostgreSQL)** | 관리형 · REST 자동 생성 · 뷰 기반 집계 |
| 배포 | **Cloudflare Pages** | git 연동 자동배포 · 글로벌 CDN · 서버리스 |
| 인증 | **Cloudflare Access** | 코드 수정 없이 사내 접근통제 · 이메일 OTP |
| 수집 | **Python + GitHub Actions** | 유연한 스크래핑 · 스케줄·로그·버전관리 내장 |

**핵심 설계 원칙**
- **서버리스** — 상시 구동 서버가 없어 운영 부담·장애점 최소화
- **정적 우선** — 대시보드는 정적 빌드로 배포, 데이터만 클라이언트에서 조회 → 빠르고 저렴
- **선언적 배포** — `git push` 한 번으로 빌드·배포 자동화 (수동 배포 절차 없음)

---

## 4. 데이터 파이프라인

### 4.1 수집 대상

| 도메인 | 주요 소스 | 갱신 주기 |
|---|---|---|
| 거시경제 지표 | World Bank · BSP · PSA OpenSTAT | 지표별(월/분기/연) |
| 경쟁사 가격 | 7개 거래선 (Abenson·SM·Anson's·Robinsons·Western·Emcor·Addessa) | 일 |
| 에너지효율 | DOE 에너지라벨 | 정기 |
| 경쟁 광고 | 경쟁사 광고 게시판 | 일 |

### 4.2 정제·분류 로직

수집 원본은 그대로 쓰지 않고 **다층 정규화**를 거친다.
- **모델코드 정규화** — 표기 편차가 큰 모델명을 표준 코드로 통일(canonCode)
- **카테고리 분류** — 브랜드코드 맵핑 + 휴리스틱 다층 폴백(냉장고·세탁기·TV·RAC·SAC·오디오)
- **DOE 매칭** — 에너지효율 등급을 모델에 조인(★등급)
- **노이즈 제외** — 사이니지·액세서리 등 분석 무관 항목 배제

> 이 정제 로직이 **본 시스템의 핵심 자산**이며, 단순 로우코드 툴로 대체하기 어려운
> 도메인 지식이 축적된 부분이다.

### 4.3 규모 (2026-08 기준)

- 경쟁사 가격 원본 누적: 약 75,000건
- 서빙 뷰 기준: 약 13,800건
- 일 신규 수집: 약 4,600건/일

---

## 5. 보안 · 접근 통제

- **인증 방식** — Cloudflare Access, 이메일 One-time PIN(OTP)
- **접근 대상** — 사내 도메인(@lge.com) 화이트리스트 + 지정 관리자 계정
- **세션 정책** — 1개월 유효(업무 편의) · 정책 변경으로 강화 가능
- **데이터 보호** — DB 직접 노출 없이 제한된 뷰(View)만 서빙
- **전송 구간** — 전 구간 HTTPS(Cloudflare)

---

## 6. 배포 · 운영

| 항목 | 방식 |
|---|---|
| 배포 | `git push` → Cloudflare Pages 자동 빌드·배포 (약 90초) |
| 롤백 | git 커밋 단위 · Cloudflare 배포 히스토리에서 즉시 복구 |
| 수집 실행 | GitHub Actions 스케줄 · 실행 이력·로그 자동 보존 |
| 모니터링 | 데이터 신선도(freshness) 대시보드 내장 |

---

## 7. 유지보수성 · 확장성 (조직 자산화 관점)

> 본사 표준(n8n 등 로우코드 자동화) 대비 본 시스템의 위치와 유지보수 방침.

### 7.1 레이어별 유지보수 리스크

| 레이어 | 리스크 | 대응 |
|---|---|---|
| ① 제품 (Next.js) | 낮음 — 표준 웹 스택, 인력 확보 용이 | — |
| ② 배포·인증 | 낮음 — 서버리스·설정 기반 | — |
| ③ 저장 (Supabase) | 낮음 — 관리형 서비스 | — |
| ④ 수집 (Python) | **중간** — 도메인 로직 포함 | 아래 7.2 |

### 7.2 수집 레이어 유지보수 방침

본사가 지적하는 "파이썬 유지보수 부담"은 통상 **개인 PC에서 돌아가는 속인화된
스크립트**를 가리킨다. 본 시스템은 이를 다음으로 완화한다.

1. **인프라화** — 개인 환경이 아닌 **GitHub Actions**에서 스케줄·로그·버전관리로 실행
2. **역할 분리** — 단순 수집(뉴스·환율)과 복잡 파싱(가격 매칭·분류)을 구분
3. **이관 로드맵** — 단순 수집부는 향후 **n8n 등 사내 표준으로 이관 검토**,
   도메인 로직(가격 매칭·분류)은 정밀도 유지를 위해 코드로 존치
4. **문서화** — 담당자 교체 시 인수인계 가능한 운영 문서 정비 (Phase 2)

> **결론:** 로우코드 자동화(n8n)는 "배관(데이터 이동)"에 강점이 있고, 본 시스템은
> 그 위에 얹히는 **완성 제품**이다. 두 접근은 대체 관계가 아니라 **계층 관계**이며,
> 수집 레이어를 표준에 맞춰 정렬함으로써 조직 자산으로 안착시킬 수 있다.

---

## 8. 향후 로드맵 (요약)

| 단계 | 시기 | 핵심 |
|---|---|---|
| Phase 1 | ~8월 | 전체 영문화 · 로그인 브랜딩 · 서밋 자산 |
| Phase 2 | ~4분기 | 수집기 n8n 이관 검토 · 관측성 · 보안 강화 · 문서화 |
| Phase 3 | 내년 | 분석 심화 · 신규 도메인 · 인사이트 자동발행 · 타 법인 템플릿화 |

_상세 로드맵은 `ROADMAP.md` 참조._

---

## 부록 — 용어

| 용어 | 설명 |
|---|---|
| n8n | 노드 연결 방식의 로우코드 워크플로 자동화 툴 |
| 서버리스 | 상시 구동 서버 없이 요청 시에만 실행되는 배포 방식 |
| 정적 export | 미리 빌드된 HTML/JS를 CDN에서 서빙하는 방식 |
| 뷰(View) | DB 원본을 가공해 필요한 형태로 제공하는 조회 전용 객체 |
