# 0006. 데이터 헬스 게이트(조용한 실패 감지)

- 상태: 채택됨
- 날짜: 2026-08-01

## 맥락
스크래퍼가 실패해도 다음날 데이터 결측을 눈으로 봐야 알았다(조용한 실패).

## 결정
`v_data_health` 뷰(유통별 오늘 행수·7일평균 대비 FAIL/WARN/OK·전체 staleness) + `scripts/data-health.mjs` + `data-health.yml`(매일). 이상 시 잡 실패 → GitHub 자동 이메일. Slack/Sentry/Better Stack은 시크릿만 넣으면 연동(플러그인).

## 대안
- 외부 모니터링만: 도메인 특화(유통별 급감) 감지 불가.

## 결과
- 장점: 무료로 조용한 실패 차단, 외부 툴 확장 가능.
- 트레이드오프: 임계값(7일평균 대비 40%/70%)은 데이터 특성 변화 시 조정 필요.
